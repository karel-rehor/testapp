version: 2.1

parameters:
  default-node-image:
    type: string
    default: "cimg/node:25.9"

commands:
  init-dependencies:
    steps:
      - run:
          name: "Install Dependencies"
          command: npm install

jobs:
  tests:
    steps:
      - checkout

workflow:
  build:
    jobs:
      - tests: